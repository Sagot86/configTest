server:
  port: 8189
  servlet:
    context-path: /shop
security:
  oauth2:
    resource:
      userInfoUri: ${EUREKA_URI:http://eureka-security-service/user}
    client:
      client-id: secureClientToken
      client-secret: verySecretWord
      authorized-grant-types: authorization_code
      accessTokenUri: ${EUREKA_URI:http://eureka-security-service/oauth/token}
      userAuthorizationUri: ${EUREKA_URI:http://eureka-security-service/oauth/authorize}
      tokenName: my_token
      scope: webclient